package dev.artixdev.libs.com.mongodb.client.model;

public enum ReturnDocument {
   BEFORE,
   AFTER;

   // $FF: synthetic method
   private static ReturnDocument[] $values() {
      return new ReturnDocument[]{BEFORE, AFTER};
   }
}
