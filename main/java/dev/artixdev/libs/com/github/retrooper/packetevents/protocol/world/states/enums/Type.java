package dev.artixdev.libs.com.github.retrooper.packetevents.protocol.world.states.enums;

public enum Type {
   BOTTOM,
   DOUBLE,
   LEFT,
   NORMAL,
   RIGHT,
   SINGLE,
   STICKY,
   TOP;

   // $FF: synthetic method
   private static Type[] $values() {
      return new Type[]{BOTTOM, DOUBLE, LEFT, NORMAL, RIGHT, SINGLE, STICKY, TOP};
   }
}
